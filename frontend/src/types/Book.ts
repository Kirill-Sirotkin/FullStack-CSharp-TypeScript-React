interface Book {
  id: string;
  title: string;
  isbn: string;
  description: string;
  publishedDate: string;
  authorNames: string[];
  authorIds: string[];
  quantity: number;
}
export default Book;
